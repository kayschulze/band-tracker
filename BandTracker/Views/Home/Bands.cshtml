@{
    Layout = "_Layout";
}

<h2><em>Bands</em></h2>

<div class="displayinfo">

@if (@Model.Count == 0)
{
    <h3>No Bands Have Been Added!</h3>
}

@foreach (var band in Model)
    {
        <h3><a href="/bands/@band.GetId()">@band.GetName()</a></h3>
    }

</div>
